<%= form_for :import, url: feed_imports_path, html: { multipart: true, class: "imports-form" } do |f| %>
  <%= f.label :document, "choose file", class: "imports-label" %>
  <%= f.file_field :document, class: "imports-input" %>
  <%= f.submit "importează", class: "button imports-submit", disabled: true %>
<% end %>
